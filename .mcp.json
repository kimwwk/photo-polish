{
  "mcpServers": {
    "kie": {
      "command": "bash",
      "args": [
        "-c",
        "if [ -z \"${KIE_AI_API_KEY:-}\" ] && [ -f .env ]; then set -a; . ./.env; set +a; fi; exec npx -y @felores/kie-ai-mcp-server"
      ],
      "env": {
        "KIE_AI_ENABLED_TOOLS": "nano_banana_image,flux_kontext,flux_2,gpt_image_2,topaz"
      }
    }
  }
}
